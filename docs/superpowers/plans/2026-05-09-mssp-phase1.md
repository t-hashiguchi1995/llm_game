# MSSP Phase 1 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** React + Vite フロントエンドとして My Sweet Secret Princess Phase 1（固定シナリオADV）を構築し、GitHub Pages に自動デプロイする CI/CD を整備する。

**Architecture:** `frontend/` サブディレクトリに Vite + React + TypeScript + Tailwind v4 + Zustand アプリを構築する。ゲームロジックは `engine/scenarioEngine.ts`（純粋関数）に分離し Vitest でテストする。Zustand ストアがエンジン関数を呼び出し、React コンポーネントはストアのみ参照する。

**Tech Stack:** React 19, Vite 6, Tailwind CSS 4, Zustand 5, TypeScript 5, Vitest, ESLint (flat config), peaceiris/actions-gh-pages

---

## ファイルマップ

| ファイル | 役割 |
|---|---|
| `frontend/src/types/index.ts` | 全型定義 |
| `frontend/src/engine/scenarioEngine.ts` | 純粋関数（resolveEvent, checkEnding, clampParam） |
| `frontend/src/engine/scenarioEngine.test.ts` | Vitest ユニットテスト |
| `frontend/src/store/useGameStore.ts` | Zustand ストア（ゲーム状態全般） |
| `frontend/src/hooks/useSaveLoad.ts` | localStorage セーブ・ロード |
| `frontend/src/data/scenarios.json` | 20イベント定義 |
| `frontend/src/data/endings.json` | 4エンディング定義 |
| `frontend/src/data/characters.json` | リゼ立ち絵・emotion_map |
| `frontend/src/data/commands.json` | 20コマンド定義 |
| `frontend/src/components/StatusBar.tsx` | 上部ゲージ3本 + SAVE/LOG/MENUボタン |
| `frontend/src/components/CharacterSprite.tsx` | 立ち絵表示（emotion → sprite解決） |
| `frontend/src/components/DialogBox.tsx` | 話者名 + セリフ表示 |
| `frontend/src/components/SceneView.tsx` | 背景 + CharacterSprite + DialogBox |
| `frontend/src/components/CommandPanel.tsx` | カテゴリ別ボタン + クールダウンバー |
| `frontend/src/components/SaveLoadModal.tsx` | 6スロット セーブ/ロード UI |
| `frontend/src/components/EndingScreen.tsx` | エンディング表示（テキスト + リセット） |
| `frontend/src/components/GameScreen.tsx` | 全体レイアウト制御 |
| `frontend/src/App.tsx` | エントリーポイント |
| `frontend/public/images/` | 背景・立ち絵（コピー先） |
| `.github/workflows/ci.yml` | secret-scan + frontend-ci |
| `.github/workflows/deploy.yml` | main push → gh-pages デプロイ |
| `lefthook.yml` | tsc-check / eslint-check フック追加 |

---

## Task 1: Vite プロジェクト初期化

**Files:**
- Create: `frontend/` (Vite scaffold)
- Create: `frontend/vite.config.ts`
- Create: `frontend/tsconfig.json`
- Create: `frontend/eslint.config.js`

- [ ] **Step 1: Vite プロジェクトを scaffold**

```bash
cd /root/workspace/llm_game
npm create vite@latest frontend -- --template react-ts
cd frontend
```

- [ ] **Step 2: 依存関係をインストール**

```bash
cd /root/workspace/llm_game/frontend
npm install zustand
npm install -D tailwindcss @tailwindcss/vite vitest @vitest/ui jsdom @testing-library/react @testing-library/jest-dom typescript-eslint eslint @eslint/js eslint-plugin-react-hooks
```

- [ ] **Step 3: `vite.config.ts` を上書き**

```typescript
import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'
import tailwindcss from '@tailwindcss/vite'

export default defineConfig({
  plugins: [react(), tailwindcss()],
  base: '/llm_game/',
  test: {
    environment: 'jsdom',
    setupFiles: ['./src/test-setup.ts'],
    globals: true,
  },
})
```

- [ ] **Step 4: `frontend/src/test-setup.ts` を作成**

```typescript
import '@testing-library/jest-dom'
```

- [ ] **Step 5: `frontend/src/index.css` を Tailwind v4 用に上書き**

```css
@import "tailwindcss";

:root {
  --color-bg: #0d0d1a;
}

html {
  background-color: var(--color-bg);
}
```

- [ ] **Step 6: `eslint.config.js` を作成**

```javascript
import js from '@eslint/js'
import tseslint from 'typescript-eslint'
import reactHooks from 'eslint-plugin-react-hooks'

export default tseslint.config(
  js.configs.recommended,
  ...tseslint.configs.recommended,
  {
    plugins: { 'react-hooks': reactHooks },
    rules: { ...reactHooks.configs.recommended.rules },
  }
)
```

- [ ] **Step 7: `package.json` の scripts を確認・更新**

`frontend/package.json` の `scripts` セクションが以下になっていることを確認：
```json
{
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "lint": "eslint .",
    "preview": "vite preview",
    "test": "vitest run",
    "test:ui": "vitest --ui"
  }
}
```

- [ ] **Step 8: 動作確認**

```bash
cd /root/workspace/llm_game/frontend && npm run build
```

Expected: `dist/` が生成され、エラーなし。

- [ ] **Step 9: コミット**

```bash
cd /root/workspace/llm_game
git add frontend/
git commit -m "feat: Vite + React + Tailwind v4 + Vitest プロジェクト初期化"
```

---

## Task 2: 型定義

**Files:**
- Create: `frontend/src/types/index.ts`

- [ ] **Step 1: `frontend/src/types/index.ts` を作成**

```typescript
export interface GameParameters {
  sweetness: number;
  curiosity: number;
  trust: number;
}

export interface ParameterCondition {
  min?: number;
  max?: number;
}

export interface EventCondition {
  sweetness?: ParameterCondition;
  curiosity?: ParameterCondition;
  trust?: ParameterCondition;
  flags?: string[];
}

export interface EventTrigger {
  type: 'command' | 'parameter' | 'flag';
  command_id?: string;
}

export interface ParameterDelta {
  sweetness: number;
  curiosity: number;
  trust: number;
}

export interface ScenarioEvent {
  id: string;
  trigger: EventTrigger;
  condition: EventCondition;
  scene_context: string;
  emotion: string;
  background: string;
  fallback_text: string;
  parameter_delta: ParameterDelta;
  set_flags: string[];
  next_event: string | null;
}

export interface EndingCondition {
  sweetness?: ParameterCondition;
  curiosity?: ParameterCondition;
  trust?: ParameterCondition;
}

export interface Ending {
  id: string;
  label: string;
  condition: EndingCondition;
  priority: number;
  cg: string | null;
  text: string;
}

export interface CharacterSprites {
  [key: string]: string;
}

export interface Character {
  id: string;
  name: string;
  sprites: CharacterSprites;
  emotion_map: Record<string, string>;
}

export interface Command {
  id: string;
  label: string;
  category: string;
}

export interface CurrentDialog {
  text: string;
  speaker: string;
  emotion: string;
  background: string;
}

export interface SaveSlot {
  slot: number;
  timestamp: string;
  params: GameParameters;
  currentScene: string;
  flags: string[];
  isEnded: boolean;
  endingId: string | null;
  currentDialog: CurrentDialog | null;
}

export interface GameState {
  params: GameParameters;
  currentScene: string;
  flags: string[];
  elapsedTime: number;
  saveSlots: SaveSlot[];
  isEnded: boolean;
  endingId: string | null;
  currentDialog: CurrentDialog | null;
  cooldownUntil: number;
}
```

- [ ] **Step 2: tsc でエラーなし確認**

```bash
cd /root/workspace/llm_game/frontend && npx tsc --noEmit
```

Expected: エラーなし。

- [ ] **Step 3: コミット**

```bash
cd /root/workspace/llm_game
git add frontend/src/types/
git commit -m "feat: ゲーム型定義 (GameState, ScenarioEvent, Ending, Character)"
```

---

## Task 3: JSON データファイル

**Files:**
- Create: `frontend/src/data/commands.json`
- Create: `frontend/src/data/scenarios.json`
- Create: `frontend/src/data/endings.json`
- Create: `frontend/src/data/characters.json`

- [ ] **Step 1: `frontend/src/data/commands.json` を作成**

```json
{
  "commands": [
    { "id": "cmd-talk-001", "label": "今日の配信どうだった？", "category": "会話" },
    { "id": "cmd-talk-002", "label": "好きなもの聞かせて", "category": "会話" },
    { "id": "cmd-talk-003", "label": "最近何か悩んでる？", "category": "会話" },
    { "id": "cmd-talk-004", "label": "一緒に何かしたいな", "category": "会話" },
    { "id": "cmd-talk-005", "label": "夜更かしが多いね", "category": "会話" },
    { "id": "cmd-touch-001", "label": "隣に座ってもいい？", "category": "スキンシップ" },
    { "id": "cmd-touch-002", "label": "手を繋いでもいい？", "category": "スキンシップ" },
    { "id": "cmd-touch-003", "label": "頭撫でていい？", "category": "スキンシップ" },
    { "id": "cmd-touch-004", "label": "抱きしめていい？", "category": "スキンシップ" },
    { "id": "cmd-touch-005", "label": "もっと近くに来て", "category": "スキンシップ" },
    { "id": "cmd-support-001", "label": "疲れてるなら休もうか", "category": "サポート" },
    { "id": "cmd-support-002", "label": "飲み物持ってくるね", "category": "サポート" },
    { "id": "cmd-support-003", "label": "一緒に寝てもいいよ", "category": "サポート" },
    { "id": "cmd-support-004", "label": "悩みがあるなら聞くよ", "category": "サポート" },
    { "id": "cmd-support-005", "label": "明日は早く寝ようね", "category": "サポート" },
    { "id": "cmd-suggest-001", "label": "着替えを手伝おうか", "category": "提案" },
    { "id": "cmd-suggest-002", "label": "映画でも観ない？", "category": "提案" },
    { "id": "cmd-suggest-003", "label": "一緒にお風呂入る？", "category": "提案" },
    { "id": "cmd-suggest-004", "label": "そっちに行っていい？", "category": "提案" },
    { "id": "cmd-suggest-005", "label": "明日も会えるかな", "category": "提案" }
  ]
}
```

- [ ] **Step 2: `frontend/src/data/scenarios.json` を作成**

```json
{
  "events": [
    {
      "id": "evt-talk-001",
      "trigger": { "type": "command", "command_id": "cmd-talk-001" },
      "condition": { "flags": [] },
      "scene_context": "プレイヤーが今日の配信について聞いてきた",
      "emotion": "normal_1",
      "background": "男性一人部屋３（夜・照明ON）.jpg",
      "fallback_text": "え、今日の配信？うーん、いつも通りかな。でもね、コメントがすごく盛り上がって楽しかったよ。",
      "parameter_delta": { "sweetness": 8, "trust": 5, "curiosity": 0 },
      "set_flags": [],
      "next_event": null
    },
    {
      "id": "evt-talk-002",
      "trigger": { "type": "command", "command_id": "cmd-talk-002" },
      "condition": { "flags": [] },
      "scene_context": "プレイヤーが好きなものを聞いてきた",
      "emotion": "happy_2",
      "background": "男性一人部屋３（夜・照明ON）.jpg",
      "fallback_text": "好きなもの？そうだな〜、紅茶と、あと読書かな。深夜に静かに本読むのが一番落ち着く。",
      "parameter_delta": { "sweetness": 6, "trust": 3, "curiosity": 0 },
      "set_flags": [],
      "next_event": null
    },
    {
      "id": "evt-talk-003",
      "trigger": { "type": "command", "command_id": "cmd-talk-003" },
      "condition": { "trust": { "min": 40 }, "flags": [] },
      "scene_context": "信頼度が高い状態でプレイヤーが悩みを聞いてきた",
      "emotion": "sad_1",
      "background": "男性一人部屋３（夜・照明ON）.jpg",
      "fallback_text": "悩み…ないわけじゃないけど。こんな時間に話してもいい？…ちょっと、配信について考えてることがあって。",
      "parameter_delta": { "sweetness": 3, "trust": 10, "curiosity": 0 },
      "set_flags": ["trust_talk_done"],
      "next_event": null
    },
    {
      "id": "evt-talk-003-low",
      "trigger": { "type": "command", "command_id": "cmd-talk-003" },
      "condition": { "trust": { "max": 39 }, "flags": [] },
      "scene_context": "信頼度が低い状態でプレイヤーが悩みを聞いてきた",
      "emotion": "normal_3",
      "background": "男性一人部屋３（夜・照明ON）.jpg",
      "fallback_text": "悩み？…うーん、特にないかな。（少し目を逸らす）",
      "parameter_delta": { "sweetness": 0, "trust": 2, "curiosity": 0 },
      "set_flags": [],
      "next_event": null
    },
    {
      "id": "evt-talk-004",
      "trigger": { "type": "command", "command_id": "cmd-talk-004" },
      "condition": { "flags": [] },
      "scene_context": "プレイヤーが一緒に何かしたいと言ってきた",
      "emotion": "happy_1",
      "background": "男性一人部屋３（夜・照明ON）.jpg",
      "fallback_text": "一緒に何か？ふふ、この時間に何するの。…まあ、一緒にいるだけでも悪くないけど。",
      "parameter_delta": { "sweetness": 10, "trust": 2, "curiosity": 2 },
      "set_flags": [],
      "next_event": null
    },
    {
      "id": "evt-talk-005",
      "trigger": { "type": "command", "command_id": "cmd-talk-005" },
      "condition": { "flags": [] },
      "scene_context": "プレイヤーが夜更かしを心配してきた",
      "emotion": "embarrassed_1",
      "background": "男性一人部屋３（夜・照明ON）.jpg",
      "fallback_text": "うっ、そこ突いてくる？わかってるんだけどね…なんか眠れなくて。あなたが起きてるからっていうのもあるかも。",
      "parameter_delta": { "sweetness": 5, "trust": 5, "curiosity": 0 },
      "set_flags": [],
      "next_event": null
    },
    {
      "id": "evt-touch-001",
      "trigger": { "type": "command", "command_id": "cmd-touch-001" },
      "condition": { "flags": [] },
      "scene_context": "プレイヤーが隣に座りたいと言ってきた",
      "emotion": "normal_3",
      "background": "男性一人部屋３（夜・照明ON）.jpg",
      "fallback_text": "…いいよ、別に。べつに嬉しくないし。（少し詰めて場所を作る）",
      "parameter_delta": { "sweetness": 5, "trust": 2, "curiosity": 3 },
      "set_flags": [],
      "next_event": null
    },
    {
      "id": "evt-touch-002",
      "trigger": { "type": "command", "command_id": "cmd-touch-002" },
      "condition": { "sweetness": { "min": 40 }, "flags": [] },
      "scene_context": "親愛度が高い状態でプレイヤーが手を繋ぎたいと言ってきた",
      "emotion": "embarrassed_2",
      "background": "男性一人部屋３（夜・照明ON）.jpg",
      "fallback_text": "て、手？…まあ、いいけど。寒いし。（小声）寒いからね。",
      "parameter_delta": { "sweetness": 8, "trust": -2, "curiosity": 5 },
      "set_flags": [],
      "next_event": null
    },
    {
      "id": "evt-touch-002-low",
      "trigger": { "type": "command", "command_id": "cmd-touch-002" },
      "condition": { "sweetness": { "max": 39 }, "flags": [] },
      "scene_context": "親愛度が低い状態でプレイヤーが手を繋ぎたいと言ってきた",
      "emotion": "scared_1",
      "background": "男性一人部屋３（夜・照明ON）.jpg",
      "fallback_text": "ちょっと待って。まだそんな仲じゃないと思うんだけど。",
      "parameter_delta": { "sweetness": 0, "trust": -5, "curiosity": 0 },
      "set_flags": [],
      "next_event": null
    },
    {
      "id": "evt-touch-003",
      "trigger": { "type": "command", "command_id": "cmd-touch-003" },
      "condition": { "sweetness": { "min": 55 }, "flags": [] },
      "scene_context": "親愛度が高い状態でプレイヤーが頭を撫でたいと言ってきた",
      "emotion": "embarrassed_4",
      "background": "男性一人部屋３（夜・照明ON）.jpg",
      "fallback_text": "な、撫でる？…っ、子ども扱いしないでよ。…でも、ちょっとだけならいいです。",
      "parameter_delta": { "sweetness": 10, "trust": 2, "curiosity": 3 },
      "set_flags": [],
      "next_event": null
    },
    {
      "id": "evt-touch-003-low",
      "trigger": { "type": "command", "command_id": "cmd-touch-003" },
      "condition": { "sweetness": { "max": 54 }, "flags": [] },
      "scene_context": "親愛度が低い状態でプレイヤーが頭を撫でようとした",
      "emotion": "normal_5",
      "background": "男性一人部屋３（夜・照明ON）.jpg",
      "fallback_text": "え、なんで？…距離感バグってない？",
      "parameter_delta": { "sweetness": 0, "trust": -3, "curiosity": 0 },
      "set_flags": [],
      "next_event": null
    },
    {
      "id": "evt-touch-004",
      "trigger": { "type": "command", "command_id": "cmd-touch-004" },
      "condition": { "curiosity": { "min": 20 }, "sweetness": { "min": 50 }, "flags": [] },
      "scene_context": "開拓度・親愛度が高い状態でプレイヤーが抱きしめたいと言ってきた",
      "emotion": "embarrassed_5",
      "background": "男性一人部屋３（夜・照明ON）.jpg",
      "fallback_text": "…っ！い、急に何言って…。（しばらく沈黙）…もう。来てもいいです。",
      "parameter_delta": { "sweetness": 5, "trust": -3, "curiosity": 8 },
      "set_flags": [],
      "next_event": null
    },
    {
      "id": "evt-touch-004-low",
      "trigger": { "type": "command", "command_id": "cmd-touch-004" },
      "condition": { "flags": [] },
      "scene_context": "条件未達でプレイヤーが抱きしめようとした",
      "emotion": "scared_1",
      "background": "男性一人部屋３（夜・照明ON）.jpg",
      "fallback_text": "…ちょっと待って。それは、まだ早いと思う。",
      "parameter_delta": { "sweetness": 0, "trust": -8, "curiosity": 0 },
      "set_flags": [],
      "next_event": null
    },
    {
      "id": "evt-touch-005",
      "trigger": { "type": "command", "command_id": "cmd-touch-005" },
      "condition": { "flags": [] },
      "scene_context": "プレイヤーがもっと近くに来てと言ってきた",
      "emotion": "normal_2",
      "background": "男性一人部屋３（夜・照明ON）.jpg",
      "fallback_text": "…近く？（少し間があって）まあ、いいけど。寒いし。",
      "parameter_delta": { "sweetness": 3, "trust": 0, "curiosity": 3 },
      "set_flags": [],
      "next_event": null
    },
    {
      "id": "evt-support-001",
      "trigger": { "type": "command", "command_id": "cmd-support-001" },
      "condition": { "flags": [] },
      "scene_context": "プレイヤーが疲れているなら休もうかと言ってきた",
      "emotion": "scared_2",
      "background": "男性一人部屋３（夜・照明ON）.jpg",
      "fallback_text": "も、もう終わりましたから…平気です。全然こわくないです。…（少し震えている）",
      "parameter_delta": { "sweetness": 5, "trust": 10, "curiosity": 0 },
      "set_flags": ["horror_event_done"],
      "next_event": null
    },
    {
      "id": "evt-support-002",
      "trigger": { "type": "command", "command_id": "cmd-support-002" },
      "condition": { "flags": [] },
      "scene_context": "プレイヤーが飲み物を持ってきてくれると言った",
      "emotion": "happy_3",
      "background": "男性一人部屋３（夜・照明ON）.jpg",
      "fallback_text": "え、いいの？じゃあ紅茶がいいな。ちゃんと温かいやつ。ありがとう、気が利くね。",
      "parameter_delta": { "sweetness": 3, "trust": 8, "curiosity": 0 },
      "set_flags": [],
      "next_event": null
    },
    {
      "id": "evt-support-003",
      "trigger": { "type": "command", "command_id": "cmd-support-003" },
      "condition": { "trust": { "min": 40 }, "flags": [] },
      "scene_context": "信頼度が高い状態でプレイヤーが一緒に寝てもいいと言ってきた",
      "emotion": "embarrassed_3",
      "background": "男性一人部屋３（夜・照明ON）.jpg",
      "fallback_text": "い、一緒に…？あなたってたまにこういうこと言うよね。……眠れないのは本当だし、まあ、いいけど。",
      "parameter_delta": { "sweetness": 8, "trust": 12, "curiosity": 0 },
      "set_flags": [],
      "next_event": null
    },
    {
      "id": "evt-support-003-low",
      "trigger": { "type": "command", "command_id": "cmd-support-003" },
      "condition": { "trust": { "max": 39 }, "flags": [] },
      "scene_context": "信頼度が低い状態でプレイヤーが一緒に寝ようと言ってきた",
      "emotion": "scared_1",
      "background": "男性一人部屋３（夜・照明ON）.jpg",
      "fallback_text": "…えっ、急になんで。ちゃんと信頼関係ができてから言ってほしい。",
      "parameter_delta": { "sweetness": 0, "trust": -5, "curiosity": 0 },
      "set_flags": [],
      "next_event": null
    },
    {
      "id": "evt-support-004",
      "trigger": { "type": "command", "command_id": "cmd-support-004" },
      "condition": { "flags": [] },
      "scene_context": "プレイヤーが悩みを聞くと言ってきた",
      "emotion": "sad_2",
      "background": "男性一人部屋３（夜・照明ON）.jpg",
      "fallback_text": "悩み…聞いてくれる？実はちょっと、最近スランプみたいな感じがして。配信に気合が入らない日があって。",
      "parameter_delta": { "sweetness": 5, "trust": 8, "curiosity": 0 },
      "set_flags": [],
      "next_event": null
    },
    {
      "id": "evt-support-005",
      "trigger": { "type": "command", "command_id": "cmd-support-005" },
      "condition": { "flags": [] },
      "scene_context": "プレイヤーが明日は早く寝るよう言ってきた",
      "emotion": "normal_2",
      "background": "男性一人部屋３（夜・照明ON）.jpg",
      "fallback_text": "うぅ、わかってる。でもこの時間って妙に冴えてくるんだよね。…明日は頑張ってみる。",
      "parameter_delta": { "sweetness": 0, "trust": 5, "curiosity": 0 },
      "set_flags": [],
      "next_event": null
    },
    {
      "id": "evt-suggest-001",
      "trigger": { "type": "command", "command_id": "cmd-suggest-001" },
      "condition": { "curiosity": { "min": 10 }, "flags": [] },
      "scene_context": "開拓度が一定以上でプレイヤーが着替えを手伝うと言ってきた",
      "emotion": "embarrassed_5",
      "background": "男性一人部屋３（夜・照明ON）.jpg",
      "fallback_text": "はあ？！な、何言って…！自分でできます！絶対に、ぜったいにできます！（顔が赤い）",
      "parameter_delta": { "sweetness": 0, "trust": -3, "curiosity": 8 },
      "set_flags": [],
      "next_event": null
    },
    {
      "id": "evt-suggest-001-low",
      "trigger": { "type": "command", "command_id": "cmd-suggest-001" },
      "condition": { "curiosity": { "max": 9 }, "flags": [] },
      "scene_context": "開拓度が低い状態でプレイヤーが着替えを手伝うと言ってきた",
      "emotion": "normal_5",
      "background": "男性一人部屋３（夜・照明ON）.jpg",
      "fallback_text": "…え？何が言いたいの。意味わかんない。",
      "parameter_delta": { "sweetness": 0, "trust": -3, "curiosity": 0 },
      "set_flags": [],
      "next_event": null
    },
    {
      "id": "evt-suggest-002",
      "trigger": { "type": "command", "command_id": "cmd-suggest-002" },
      "condition": { "flags": [] },
      "scene_context": "プレイヤーが映画を観ようと言ってきた",
      "emotion": "happy_4",
      "background": "男性一人部屋３（夜・照明ON）.jpg",
      "fallback_text": "映画！いいね。ホラーはなしで。アクションかコメディがいい。…一緒に観るのって久しぶりだね。",
      "parameter_delta": { "sweetness": 5, "trust": 3, "curiosity": 3 },
      "set_flags": [],
      "next_event": null
    },
    {
      "id": "evt-suggest-003",
      "trigger": { "type": "command", "command_id": "cmd-suggest-003" },
      "condition": { "curiosity": { "min": 25 }, "sweetness": { "min": 60 }, "flags": [] },
      "scene_context": "開拓度・親愛度が高い状態でプレイヤーが一緒にお風呂に入ろうと言ってきた",
      "emotion": "scared_1",
      "background": "男性一人部屋３（夜・照明ON）.jpg",
      "fallback_text": "…えっ。えっ？！な、なんで急に…！そ、そういうの考えてたの？！……すごく赤くなってる自覚はある。",
      "parameter_delta": { "sweetness": 0, "trust": -5, "curiosity": 15 },
      "set_flags": [],
      "next_event": null
    },
    {
      "id": "evt-suggest-003-low",
      "trigger": { "type": "command", "command_id": "cmd-suggest-003" },
      "condition": { "flags": [] },
      "scene_context": "条件未達でプレイヤーがお風呂を提案してきた",
      "emotion": "normal_5",
      "background": "男性一人部屋３（夜・照明ON）.jpg",
      "fallback_text": "…はい？今何て言った？聞き間違いかな。",
      "parameter_delta": { "sweetness": 0, "trust": -5, "curiosity": 0 },
      "set_flags": [],
      "next_event": null
    },
    {
      "id": "evt-suggest-004",
      "trigger": { "type": "command", "command_id": "cmd-suggest-004" },
      "condition": { "flags": [] },
      "scene_context": "プレイヤーがそっちに行っていいかと言ってきた",
      "emotion": "normal_4",
      "background": "男性一人部屋３（夜・照明ON）.jpg",
      "fallback_text": "来ていいよ、別に。広いわけじゃないけど。…ちゃんと座る場所、作っておいたから。",
      "parameter_delta": { "sweetness": 3, "trust": 5, "curiosity": 3 },
      "set_flags": [],
      "next_event": null
    },
    {
      "id": "evt-suggest-005",
      "trigger": { "type": "command", "command_id": "cmd-suggest-005" },
      "condition": { "flags": [] },
      "scene_context": "プレイヤーが明日も会えるか聞いてきた",
      "emotion": "happy_1",
      "background": "男性一人部屋３（夜・照明ON）.jpg",
      "fallback_text": "明日も…？うん、会えると思う。配信あるけど、終わったら来ていいよ。…楽しみにしてるから。",
      "parameter_delta": { "sweetness": 8, "trust": 3, "curiosity": 0 },
      "set_flags": [],
      "next_event": null
    }
  ]
}
```

- [ ] **Step 3: `frontend/src/data/endings.json` を作成**

```json
{
  "endings": [
    {
      "id": "ending-true",
      "label": "トゥルーハッピーエンド",
      "condition": {
        "sweetness": { "min": 80 },
        "curiosity": { "min": 80 },
        "trust": { "min": 80 }
      },
      "priority": 1,
      "cg": null,
      "text": "朝の光が差し込む頃、ふたりはまだ話していた。言葉は少なくなっていたけれど、その沈黙は温かかった。「また明日ね」と彼女は言った。その声は、いつもより少しだけ柔らかかった。"
    },
    {
      "id": "ending-normal",
      "label": "ノーマルハッピーエンド",
      "condition": {
        "sweetness": { "min": 70 },
        "trust": { "min": 70 },
        "curiosity": { "max": 49 }
      },
      "priority": 2,
      "cg": null,
      "text": "穏やかな夜が過ぎて、ふたりは静かに眠りについた。特別なことは何もなかったけれど、隣にいるだけで十分だった。そういう夜が、続いていけばいいと思った。"
    },
    {
      "id": "ending-dark",
      "label": "依存ダークエンド",
      "condition": {
        "curiosity": { "min": 70 },
        "trust": { "max": 30 }
      },
      "priority": 3,
      "cg": null,
      "text": "彼女の目に宿る光は、もはや純粋なものではなかった。刺激を求める眼差し。それが愛情なのか、依存なのか、もう彼女自身にも分からなかった。深夜はいつまでも続く。"
    },
    {
      "id": "ending-bad",
      "label": "バッドエンド",
      "condition": {
        "trust": { "max": 15 }
      },
      "priority": 4,
      "cg": null,
      "text": "「もう、帰って」。彼女の声は静かで、だからこそ重かった。扉が閉まる音がした。夜はまだ長いのに、部屋はもう暗かった。"
    }
  ]
}
```

- [ ] **Step 4: `frontend/src/data/characters.json` を作成**

```json
{
  "characters": [
    {
      "id": "rize",
      "name": "リゼ・ヘルエスタ",
      "sprites": {
        "normal_1": "images/characters/lize/lize-normal1.png",
        "normal_2": "images/characters/lize/lize-normal2.png",
        "normal_3": "images/characters/lize/lize-normal3.png",
        "normal_4": "images/characters/lize/lize-normal4.png",
        "normal_5": "images/characters/lize/lize-normal5.png",
        "happy_1": "images/characters/lize/lize-happy1.png",
        "happy_2": "images/characters/lize/lize-happy2.png",
        "happy_3": "images/characters/lize/lize-happy3.png",
        "happy_4": "images/characters/lize/lize-happy4.png",
        "embarrassed_1": "images/characters/lize/lize-embarrassed1.png",
        "embarrassed_2": "images/characters/lize/lize-embarrassed2.png",
        "embarrassed_3": "images/characters/lize/lize-embarrassed3.png",
        "embarrassed_4": "images/characters/lize/lize-embarrassed4.png",
        "embarrassed_5": "images/characters/lize/lize-embarrassed5.png",
        "sad_1": "images/characters/lize/lize-sad1.png",
        "sad_2": "images/characters/lize/lize-sad2.png",
        "sad_3": "images/characters/lize/lize-sad3.png",
        "scared_1": "images/characters/lize/lize-scared1.png",
        "scared_2": "images/characters/lize/lize-scared2.png"
      },
      "emotion_map": {
        "default": "normal_1",
        "normal": "normal_1",
        "happy": "happy_1",
        "embarrassed": "embarrassed_1",
        "sad": "sad_1",
        "scared": "scared_1"
      }
    }
  ]
}
```

- [ ] **Step 5: コミット**

```bash
cd /root/workspace/llm_game
git add frontend/src/data/
git commit -m "feat: シナリオ・エンディング・キャラクター・コマンド JSON データ追加"
```

---

## Task 4: シナリオエンジン（純粋関数 + テスト）

**Files:**
- Create: `frontend/src/engine/scenarioEngine.ts`
- Create: `frontend/src/engine/scenarioEngine.test.ts`

- [ ] **Step 1: テストファイルを先に作成（TDD）**

`frontend/src/engine/scenarioEngine.test.ts`:

```typescript
import { describe, it, expect } from 'vitest'
import {
  clampParam,
  resolveEvent,
  checkEnding,
  resolveSpritePath,
} from './scenarioEngine'
import type { GameParameters, ScenarioEvent, Ending, Character } from '../types'

const baseParams: GameParameters = { sweetness: 50, curiosity: 0, trust: 50 }

describe('clampParam', () => {
  it('値を0〜100に収める', () => {
    expect(clampParam(110)).toBe(100)
    expect(clampParam(-5)).toBe(0)
    expect(clampParam(50)).toBe(50)
  })
})

describe('resolveEvent', () => {
  const events: ScenarioEvent[] = [
    {
      id: 'evt-high',
      trigger: { type: 'command', command_id: 'cmd-test' },
      condition: { trust: { min: 40 }, flags: [] },
      scene_context: '',
      emotion: 'happy_1',
      background: 'bg.jpg',
      fallback_text: 'high trust response',
      parameter_delta: { sweetness: 5, curiosity: 0, trust: 5 },
      set_flags: [],
      next_event: null,
    },
    {
      id: 'evt-low',
      trigger: { type: 'command', command_id: 'cmd-test' },
      condition: { trust: { max: 39 }, flags: [] },
      scene_context: '',
      emotion: 'normal_1',
      background: 'bg.jpg',
      fallback_text: 'low trust response',
      parameter_delta: { sweetness: 0, curiosity: 0, trust: 2 },
      set_flags: [],
      next_event: null,
    },
  ]

  it('条件に合致する最初のイベントを返す', () => {
    const result = resolveEvent('cmd-test', { ...baseParams, trust: 60 }, [], events)
    expect(result?.id).toBe('evt-high')
  })

  it('trust が低い場合はフォールバックイベントを返す', () => {
    const result = resolveEvent('cmd-test', { ...baseParams, trust: 20 }, [], events)
    expect(result?.id).toBe('evt-low')
  })

  it('コマンドIDが合致しない場合は null を返す', () => {
    const result = resolveEvent('cmd-other', baseParams, [], events)
    expect(result).toBeNull()
  })

  it('フラグ条件が未充足の場合はスキップする', () => {
    const flagEvent: ScenarioEvent = {
      ...events[0],
      id: 'evt-flag',
      condition: { flags: ['required_flag'] },
    }
    const result = resolveEvent('cmd-test', baseParams, [], [flagEvent])
    expect(result).toBeNull()
  })

  it('フラグが揃っている場合はイベントを返す', () => {
    const flagEvent: ScenarioEvent = {
      ...events[0],
      id: 'evt-flag',
      condition: { flags: ['required_flag'] },
    }
    const result = resolveEvent('cmd-test', baseParams, ['required_flag'], [flagEvent])
    expect(result?.id).toBe('evt-flag')
  })
})

describe('checkEnding', () => {
  const endings: Ending[] = [
    {
      id: 'ending-true',
      label: 'トゥルー',
      condition: { sweetness: { min: 80 }, curiosity: { min: 80 }, trust: { min: 80 } },
      priority: 1,
      cg: null,
      text: '',
    },
    {
      id: 'ending-bad',
      label: 'バッド',
      condition: { trust: { max: 15 } },
      priority: 4,
      cg: null,
      text: '',
    },
  ]

  it('条件を満たすエンディングを priority 順に返す', () => {
    const result = checkEnding({ sweetness: 85, curiosity: 85, trust: 85 }, endings)
    expect(result?.id).toBe('ending-true')
  })

  it('バッドエンド条件（trust <= 15）を検出する', () => {
    const result = checkEnding({ sweetness: 30, curiosity: 10, trust: 10 }, endings)
    expect(result?.id).toBe('ending-bad')
  })

  it('条件未充足の場合は null を返す', () => {
    const result = checkEnding(baseParams, endings)
    expect(result).toBeNull()
  })
})

describe('resolveSpritePath', () => {
  const character: Character = {
    id: 'rize',
    name: 'リゼ',
    sprites: { normal_1: 'images/normal1.png', happy_1: 'images/happy1.png' },
    emotion_map: { default: 'normal_1', happy: 'happy_1' },
  }

  it('直接キーで立ち絵パスを解決する', () => {
    expect(resolveSpritePath(character, 'normal_1')).toBe('images/normal1.png')
  })

  it('emotion_map 経由で立ち絵パスを解決する', () => {
    expect(resolveSpritePath(character, 'happy')).toBe('images/happy1.png')
  })

  it('未知の感情は default にフォールバックする', () => {
    expect(resolveSpritePath(character, 'unknown')).toBe('images/normal1.png')
  })
})
```

- [ ] **Step 2: テストが失敗することを確認**

```bash
cd /root/workspace/llm_game/frontend && npm test
```

Expected: `Cannot find module './scenarioEngine'` エラーで失敗。

- [ ] **Step 3: `frontend/src/engine/scenarioEngine.ts` を実装**

```typescript
import type { GameParameters, ScenarioEvent, Ending, Character, ParameterCondition } from '../types'

export function clampParam(val: number): number {
  return Math.min(100, Math.max(0, val))
}

function meetsCondition(val: number, cond: ParameterCondition): boolean {
  if (cond.min !== undefined && val < cond.min) return false
  if (cond.max !== undefined && val > cond.max) return false
  return true
}

export function resolveEvent(
  commandId: string,
  params: GameParameters,
  flags: string[],
  events: ScenarioEvent[]
): ScenarioEvent | null {
  return (
    events.find((evt) => {
      if (evt.trigger.type !== 'command' || evt.trigger.command_id !== commandId) return false
      const { condition } = evt
      if (condition.sweetness && !meetsCondition(params.sweetness, condition.sweetness)) return false
      if (condition.curiosity && !meetsCondition(params.curiosity, condition.curiosity)) return false
      if (condition.trust && !meetsCondition(params.trust, condition.trust)) return false
      if (condition.flags?.length && !condition.flags.every((f) => flags.includes(f))) return false
      return true
    }) ?? null
  )
}

export function checkEnding(params: GameParameters, endings: Ending[]): Ending | null {
  const sorted = [...endings].sort((a, b) => a.priority - b.priority)
  return (
    sorted.find((ending) => {
      const { condition } = ending
      if (condition.sweetness && !meetsCondition(params.sweetness, condition.sweetness)) return false
      if (condition.curiosity && !meetsCondition(params.curiosity, condition.curiosity)) return false
      if (condition.trust && !meetsCondition(params.trust, condition.trust)) return false
      return true
    }) ?? null
  )
}

export function resolveSpritePath(character: Character, emotion: string): string {
  if (character.sprites[emotion]) return character.sprites[emotion]
  const mapped = character.emotion_map[emotion] ?? character.emotion_map['default']
  return character.sprites[mapped] ?? ''
}
```

- [ ] **Step 4: テストがすべて通ることを確認**

```bash
cd /root/workspace/llm_game/frontend && npm test
```

Expected: `14 tests passed`

- [ ] **Step 5: コミット**

```bash
cd /root/workspace/llm_game
git add frontend/src/engine/
git commit -m "feat: シナリオエンジン純粋関数実装 + Vitest テスト全パス"
```

---

## Task 5: Zustand ストア

**Files:**
- Create: `frontend/src/store/useGameStore.ts`

- [ ] **Step 1: `frontend/src/store/useGameStore.ts` を作成**

```typescript
import { create } from 'zustand'
import type { GameState, GameParameters, CurrentDialog, SaveSlot } from '../types'
import scenariosData from '../data/scenarios.json'
import endingsData from '../data/endings.json'
import charactersData from '../data/characters.json'
import { resolveEvent, checkEnding, clampParam, resolveSpritePath } from '../engine/scenarioEngine'

const COOLDOWN_MS = 3000
const SAVE_KEY = (slot: number) => `mssp_save_slot_${slot}`

const INITIAL_PARAMS: GameParameters = { sweetness: 50, curiosity: 0, trust: 50 }

const INITIAL_DIALOG: CurrentDialog = {
  text: 'も、もう終わりましたから…平気です。全然こわくないです。',
  speaker: 'リゼ・ヘルエスタ',
  emotion: 'scared_2',
  background: '男性一人部屋３（夜・照明ON）.jpg',
}

const INITIAL_STATE: Omit<GameState, 'saveSlots'> = {
  params: INITIAL_PARAMS,
  currentScene: 'scene-001',
  flags: [],
  elapsedTime: 0,
  isEnded: false,
  endingId: null,
  currentDialog: INITIAL_DIALOG,
  cooldownUntil: 0,
}

interface GameStore extends GameState {
  applyCommand: (commandId: string) => void
  checkAndApplyEnding: () => void
  saveGame: (slot: number) => void
  loadGame: (slot: number) => boolean
  resetGame: () => void
  isCoolingDown: () => boolean
}

export const useGameStore = create<GameStore>((set, get) => ({
  ...INITIAL_STATE,
  saveSlots: [],

  applyCommand: (commandId) => {
    const state = get()
    if (state.isEnded || state.isCoolingDown()) return

    const character = charactersData.characters[0]
    const event = resolveEvent(
      commandId,
      state.params,
      state.flags,
      scenariosData.events as Parameters<typeof resolveEvent>[3]
    )

    if (!event) return

    const newParams: GameParameters = {
      sweetness: clampParam(state.params.sweetness + event.parameter_delta.sweetness),
      curiosity: clampParam(state.params.curiosity + event.parameter_delta.curiosity),
      trust: clampParam(state.params.trust + event.parameter_delta.trust),
    }

    const newFlags = [...new Set([...state.flags, ...event.set_flags])]

    const dialog: CurrentDialog = {
      text: event.fallback_text,
      speaker: character.name,
      emotion: event.emotion,
      background: event.background,
    }

    set({
      params: newParams,
      flags: newFlags,
      currentDialog: dialog,
      cooldownUntil: Date.now() + COOLDOWN_MS,
    })

    get().checkAndApplyEnding()
  },

  checkAndApplyEnding: () => {
    const { params, isEnded } = get()
    if (isEnded) return
    const ending = checkEnding(params, endingsData.endings as Parameters<typeof checkEnding>[1])
    if (ending) {
      set({ isEnded: true, endingId: ending.id })
    }
  },

  isCoolingDown: () => Date.now() < get().cooldownUntil,

  saveGame: (slot) => {
    const state = get()
    const saveSlot: SaveSlot = {
      slot,
      timestamp: new Date().toISOString(),
      params: state.params,
      currentScene: state.currentScene,
      flags: state.flags,
      isEnded: state.isEnded,
      endingId: state.endingId,
      currentDialog: state.currentDialog,
    }
    const existing = state.saveSlots.filter((s) => s.slot !== slot)
    const newSlots = [...existing, saveSlot].sort((a, b) => a.slot - b.slot)
    set({ saveSlots: newSlots })
    localStorage.setItem(SAVE_KEY(slot), JSON.stringify(saveSlot))
  },

  loadGame: (slot) => {
    const raw = localStorage.getItem(SAVE_KEY(slot))
    if (!raw) return false
    try {
      const saveSlot: SaveSlot = JSON.parse(raw)
      set({
        params: saveSlot.params,
        currentScene: saveSlot.currentScene,
        flags: saveSlot.flags,
        isEnded: saveSlot.isEnded,
        endingId: saveSlot.endingId,
        currentDialog: saveSlot.currentDialog,
        cooldownUntil: 0,
      })
      return true
    } catch {
      return false
    }
  },

  resetGame: () => set({ ...INITIAL_STATE, saveSlots: get().saveSlots }),
}))
```

- [ ] **Step 2: tsc でエラーなし確認**

```bash
cd /root/workspace/llm_game/frontend && npx tsc --noEmit
```

Expected: エラーなし。

- [ ] **Step 3: コミット**

```bash
cd /root/workspace/llm_game
git add frontend/src/store/
git commit -m "feat: Zustand ゲームストア実装（applyCommand / saveGame / loadGame）"
```

---

## Task 6: アセットセットアップ

**Files:**
- Create: `frontend/public/images/characters/lize/` (画像コピー)
- Create: `frontend/public/images/background/` (画像コピー)

- [ ] **Step 1: public/images ディレクトリを作成して画像をコピー**

```bash
mkdir -p /root/workspace/llm_game/frontend/public/images/characters/lize
mkdir -p /root/workspace/llm_game/frontend/public/images/background
cp /root/workspace/llm_game/images/characters/lize/*.png \
   /root/workspace/llm_game/frontend/public/images/characters/lize/
cp /root/workspace/llm_game/images/background/*.jpg \
   /root/workspace/llm_game/frontend/public/images/background/
```

- [ ] **Step 2: .gitignore に画像を追加（バイナリはリポジトリ管理外）**

`/root/workspace/llm_game/.gitignore` に追記：

```
# Game assets (managed separately)
frontend/public/images/
```

- [ ] **Step 3: コミット**

```bash
cd /root/workspace/llm_game
git add .gitignore
git commit -m "chore: 画像アセットを gitignore に追加（public/images は別管理）"
```

---

## Task 7: StatusBar コンポーネント

**Files:**
- Create: `frontend/src/components/StatusBar.tsx`

- [ ] **Step 1: `frontend/src/components/StatusBar.tsx` を作成**

```tsx
import { useGameStore } from '../store/useGameStore'

interface Props {
  onSave: () => void
  onMenu: () => void
}

export function StatusBar({ onSave, onMenu }: Props) {
  const params = useGameStore((s) => s.params)

  return (
    <div className="flex items-center gap-4 px-4 py-2 bg-black/95 border-b border-purple-500/20">
      <span className="text-purple-400 text-xs font-semibold tracking-widest whitespace-nowrap">
        ◇ MSSP
      </span>

      <div className="flex items-center gap-3 flex-1">
        <GaugeItem label="♥ 親愛" value={params.sweetness} colorClass="bg-pink-400" labelClass="text-pink-400" />
        <GaugeItem label="✦ 開拓" value={params.curiosity} colorClass="bg-purple-400" labelClass="text-purple-400" />
        <GaugeItem label="🛡 安心" value={params.trust} colorClass="bg-blue-400" labelClass="text-blue-400" />
      </div>

      <div className="flex gap-2">
        <MenuButton onClick={onSave}>SAVE</MenuButton>
        <MenuButton onClick={onMenu}>MENU</MenuButton>
      </div>
    </div>
  )
}

function GaugeItem({
  label,
  value,
  colorClass,
  labelClass,
}: {
  label: string
  value: number
  colorClass: string
  labelClass: string
}) {
  return (
    <div className="flex items-center gap-2 flex-1">
      <span className={`text-[10px] whitespace-nowrap ${labelClass}`}>{label}</span>
      <div className="flex-1 h-1.5 bg-white/8 rounded-full overflow-hidden">
        <div
          className={`h-full rounded-full transition-all duration-500 ${colorClass}`}
          style={{ width: `${value}%` }}
        />
      </div>
      <span className="text-[10px] text-white/40 w-6 text-right">{value}</span>
    </div>
  )
}

function MenuButton({ onClick, children }: { onClick: () => void; children: React.ReactNode }) {
  return (
    <button
      onClick={onClick}
      className="bg-purple-500/10 border border-purple-500/30 rounded text-purple-400 text-[10px] px-2 py-1 hover:bg-purple-500/20 transition-colors"
    >
      {children}
    </button>
  )
}
```

- [ ] **Step 2: コミット**

```bash
cd /root/workspace/llm_game
git add frontend/src/components/StatusBar.tsx
git commit -m "feat: StatusBar コンポーネント（ゲージ3本 + SAVE/MENUボタン）"
```

---

## Task 8: CharacterSprite + DialogBox

**Files:**
- Create: `frontend/src/components/CharacterSprite.tsx`
- Create: `frontend/src/components/DialogBox.tsx`

- [ ] **Step 1: `frontend/src/components/CharacterSprite.tsx` を作成**

```tsx
import type { Character } from '../types'
import { resolveSpritePath } from '../engine/scenarioEngine'

interface Props {
  character: Character
  emotion: string
}

export function CharacterSprite({ character, emotion }: Props) {
  const path = resolveSpritePath(character, emotion)
  const base = import.meta.env.BASE_URL

  return (
    <div className="absolute bottom-0 left-1/2 -translate-x-1/2 h-full flex items-end pointer-events-none">
      <img
        src={`${base}${path}`}
        alt={character.name}
        className="max-h-full object-contain drop-shadow-2xl select-none"
        draggable={false}
      />
    </div>
  )
}
```

- [ ] **Step 2: `frontend/src/components/DialogBox.tsx` を作成**

```tsx
interface Props {
  speaker: string
  text: string
}

export function DialogBox({ speaker, text }: Props) {
  return (
    <div className="absolute bottom-0 left-0 right-0 bg-gradient-to-t from-black/97 to-black/85 border-t border-purple-500/25 px-4 py-3 md:px-6 md:py-4">
      <p className="text-[11px] md:text-xs text-purple-400 font-semibold tracking-wide mb-1.5">
        {speaker}
      </p>
      <p className="text-xs md:text-sm text-purple-100/90 leading-relaxed">
        {text}
      </p>
      <span className="absolute bottom-3 right-4 text-[10px] text-purple-500/60 animate-pulse">
        ▼
      </span>
    </div>
  )
}
```

- [ ] **Step 3: コミット**

```bash
cd /root/workspace/llm_game
git add frontend/src/components/CharacterSprite.tsx frontend/src/components/DialogBox.tsx
git commit -m "feat: CharacterSprite + DialogBox コンポーネント"
```

---

## Task 9: SceneView コンポーネント

**Files:**
- Create: `frontend/src/components/SceneView.tsx`

- [ ] **Step 1: `frontend/src/components/SceneView.tsx` を作成**

```tsx
import { useGameStore } from '../store/useGameStore'
import { CharacterSprite } from './CharacterSprite'
import { DialogBox } from './DialogBox'
import charactersData from '../data/characters.json'
import type { Character } from '../types'

const character = charactersData.characters[0] as Character

export function SceneView() {
  const dialog = useGameStore((s) => s.currentDialog)
  const base = import.meta.env.BASE_URL

  const bgUrl = dialog
    ? `${base}images/background/${dialog.background}`
    : `${base}images/background/男性一人部屋３（夜・照明ON）.jpg`

  const emotion = dialog?.emotion ?? 'normal_1'
  const speaker = dialog?.speaker ?? character.name
  const text = dialog?.text ?? '…'

  return (
    <div
      className="relative w-full flex-1 overflow-hidden"
      style={{
        backgroundImage: `url("${bgUrl}")`,
        backgroundSize: 'cover',
        backgroundPosition: 'center top',
      }}
    >
      {/* 背景暗幕 */}
      <div className="absolute inset-0 bg-black/20" />

      {/* 立ち絵 */}
      <div className="absolute inset-0 bottom-[22%]">
        <CharacterSprite character={character} emotion={emotion} />
      </div>

      {/* ダイアログ */}
      <DialogBox speaker={speaker} text={text} />
    </div>
  )
}
```

- [ ] **Step 2: コミット**

```bash
cd /root/workspace/llm_game
git add frontend/src/components/SceneView.tsx
git commit -m "feat: SceneView コンポーネント（背景 + 立ち絵 + ダイアログ）"
```

---

## Task 10: CommandPanel コンポーネント

**Files:**
- Create: `frontend/src/components/CommandPanel.tsx`

- [ ] **Step 1: `frontend/src/components/CommandPanel.tsx` を作成**

```tsx
import { useState, useEffect } from 'react'
import { useGameStore } from '../store/useGameStore'
import commandsData from '../data/commands.json'
import type { Command } from '../types'

const CATEGORIES = ['会話', 'スキンシップ', 'サポート', '提案'] as const
type Category = (typeof CATEGORIES)[number]

const CATEGORY_COLORS: Record<Category, { border: string; bg: string; hover: string }> = {
  会話: { border: 'border-purple-500/25', bg: 'bg-purple-500/4', hover: 'hover:bg-purple-500/12 hover:border-purple-500/50' },
  スキンシップ: { border: 'border-pink-400/25', bg: 'bg-pink-400/4', hover: 'hover:bg-pink-400/12 hover:border-pink-400/50' },
  サポート: { border: 'border-blue-400/25', bg: 'bg-blue-400/4', hover: 'hover:bg-blue-400/12 hover:border-blue-400/50' },
  提案: { border: 'border-yellow-400/25', bg: 'bg-yellow-400/4', hover: 'hover:bg-yellow-400/12 hover:border-yellow-400/50' },
}

const commands = commandsData.commands as Command[]

export function CommandPanel() {
  const applyCommand = useGameStore((s) => s.applyCommand)
  const cooldownUntil = useGameStore((s) => s.cooldownUntil)
  const isEnded = useGameStore((s) => s.isEnded)
  const [now, setNow] = useState(Date.now())

  useEffect(() => {
    if (cooldownUntil <= Date.now()) return
    const interval = setInterval(() => setNow(Date.now()), 50)
    return () => clearInterval(interval)
  }, [cooldownUntil])

  const remaining = Math.max(0, cooldownUntil - now)
  const cooldownProgress = remaining > 0 ? (remaining / 3000) * 100 : 0
  const isCooling = remaining > 0

  return (
    <div className="bg-black/98 border-t border-purple-500/15 px-3 py-2 md:px-4 md:py-3">
      {/* クールダウンバー */}
      <div className="h-0.5 bg-white/5 rounded-full mb-2 overflow-hidden">
        <div
          className="h-full bg-gradient-to-r from-purple-500 to-pink-400 rounded-full transition-all duration-50"
          style={{ width: `${cooldownProgress}%` }}
        />
      </div>

      {/* デスクトップ: 4列グリッド */}
      <div className="hidden md:grid md:grid-cols-4 gap-3">
        {CATEGORIES.map((cat) => (
          <CommandGroup
            key={cat}
            category={cat}
            commands={commands.filter((c) => c.category === cat)}
            onSelect={applyCommand}
            disabled={isCooling || isEnded}
          />
        ))}
      </div>

      {/* モバイル: 2×2グリッド（カテゴリボタン） */}
      <div className="md:hidden grid grid-cols-2 gap-2">
        {CATEGORIES.map((cat) => (
          <MobileCategoryButton
            key={cat}
            category={cat}
            commands={commands.filter((c) => c.category === cat)}
            onSelect={applyCommand}
            disabled={isCooling || isEnded}
          />
        ))}
      </div>
    </div>
  )
}

function CommandGroup({
  category,
  commands,
  onSelect,
  disabled,
}: {
  category: Category
  commands: Command[]
  onSelect: (id: string) => void
  disabled: boolean
}) {
  const colors = CATEGORY_COLORS[category]
  return (
    <div className="flex flex-col gap-1">
      <span className="text-[9px] text-white/30 uppercase tracking-wider pl-0.5">{category}</span>
      {commands.map((cmd) => (
        <button
          key={cmd.id}
          onClick={() => onSelect(cmd.id)}
          disabled={disabled}
          className={`
            rounded px-2 py-1.5 text-[11px] text-purple-100/80 text-center border
            transition-all duration-150 truncate
            ${colors.bg} ${colors.border}
            ${disabled ? 'opacity-40 cursor-not-allowed' : `cursor-pointer ${colors.hover} text-purple-100`}
          `}
        >
          {cmd.label}
        </button>
      ))}
    </div>
  )
}

function MobileCategoryButton({
  category,
  commands,
  onSelect,
  disabled,
}: {
  category: Category
  commands: Command[]
  onSelect: (id: string) => void
  disabled: boolean
}) {
  const [open, setOpen] = useState(false)
  const colors = CATEGORY_COLORS[category]

  return (
    <div className="relative">
      <button
        onClick={() => setOpen((v) => !v)}
        disabled={disabled}
        className={`
          w-full rounded-md px-3 py-2.5 text-xs text-purple-100/80 text-center border
          ${colors.bg} ${colors.border}
          ${disabled ? 'opacity-40 cursor-not-allowed' : `cursor-pointer ${colors.hover}`}
        `}
      >
        {category}
      </button>
      {open && !disabled && (
        <div className="absolute bottom-full left-0 right-0 mb-1 flex flex-col gap-1 z-10">
          {commands.map((cmd) => (
            <button
              key={cmd.id}
              onClick={() => { onSelect(cmd.id); setOpen(false) }}
              className={`
                rounded px-2 py-2 text-[11px] text-purple-100 text-left border
                ${colors.bg} ${colors.border} ${colors.hover}
              `}
            >
              {cmd.label}
            </button>
          ))}
        </div>
      )}
    </div>
  )
}
```

- [ ] **Step 2: コミット**

```bash
cd /root/workspace/llm_game
git add frontend/src/components/CommandPanel.tsx
git commit -m "feat: CommandPanel（カテゴリ別コマンド + クールダウンバー + モバイル対応）"
```

---

## Task 11: SaveLoadModal コンポーネント

**Files:**
- Create: `frontend/src/components/SaveLoadModal.tsx`

- [ ] **Step 1: `frontend/src/components/SaveLoadModal.tsx` を作成**

```tsx
import { useGameStore } from '../store/useGameStore'

const SLOT_COUNT = 6

interface Props {
  onClose: () => void
}

export function SaveLoadModal({ onClose }: Props) {
  const saveGame = useGameStore((s) => s.saveGame)
  const loadGame = useGameStore((s) => s.loadGame)
  const saveSlots = useGameStore((s) => s.saveSlots)
  const resetGame = useGameStore((s) => s.resetGame)

  function getSlot(slot: number) {
    return saveSlots.find((s) => s.slot === slot) ?? null
  }

  function handleLoad(slot: number) {
    const ok = loadGame(slot)
    if (ok) onClose()
  }

  function handleReset() {
    if (confirm('ゲームをリセットしますか？')) {
      resetGame()
      onClose()
    }
  }

  return (
    <div
      className="fixed inset-0 bg-black/80 flex items-center justify-center z-50"
      onClick={(e) => e.target === e.currentTarget && onClose()}
    >
      <div className="bg-[#0d0d1a] border border-purple-500/30 rounded-xl p-6 w-full max-w-md mx-4">
        <div className="flex justify-between items-center mb-5">
          <h2 className="text-purple-300 font-semibold tracking-wide">SAVE / LOAD</h2>
          <button onClick={onClose} className="text-purple-500/60 hover:text-purple-300 text-sm">✕</button>
        </div>

        <div className="grid grid-cols-2 gap-3 mb-5">
          {Array.from({ length: SLOT_COUNT }, (_, i) => i).map((slot) => {
            const data = getSlot(slot)
            return (
              <div key={slot} className="border border-purple-500/20 rounded-lg p-3 bg-purple-500/5">
                <p className="text-[10px] text-purple-400/60 mb-1">SLOT {slot + 1}</p>
                {data ? (
                  <>
                    <p className="text-[10px] text-purple-200/70 mb-2 truncate">
                      {new Date(data.timestamp).toLocaleString('ja-JP', { month: 'numeric', day: 'numeric', hour: '2-digit', minute: '2-digit' })}
                    </p>
                    <p className="text-[9px] text-purple-300/50 mb-2">
                      ♥{data.params.sweetness} ✦{data.params.curiosity} 🛡{data.params.trust}
                    </p>
                    <div className="flex gap-1.5">
                      <button
                        onClick={() => saveGame(slot)}
                        className="flex-1 text-[10px] py-1 rounded border border-purple-500/30 text-purple-300 hover:bg-purple-500/15"
                      >
                        上書き
                      </button>
                      <button
                        onClick={() => handleLoad(slot)}
                        className="flex-1 text-[10px] py-1 rounded border border-blue-400/30 text-blue-300 hover:bg-blue-400/15"
                      >
                        ロード
                      </button>
                    </div>
                  </>
                ) : (
                  <button
                    onClick={() => saveGame(slot)}
                    className="w-full text-[10px] py-1 rounded border border-purple-500/20 text-purple-400/50 hover:text-purple-300 hover:border-purple-500/40"
                  >
                    セーブ
                  </button>
                )}
              </div>
            )
          })}
        </div>

        <button
          onClick={handleReset}
          className="w-full text-xs py-2 rounded border border-red-500/30 text-red-400/70 hover:bg-red-500/10 hover:text-red-300"
        >
          ゲームをリセット
        </button>
      </div>
    </div>
  )
}
```

- [ ] **Step 2: コミット**

```bash
cd /root/workspace/llm_game
git add frontend/src/components/SaveLoadModal.tsx
git commit -m "feat: SaveLoadModal（6スロット セーブ/ロード UI）"
```

---

## Task 12: EndingScreen コンポーネント

**Files:**
- Create: `frontend/src/components/EndingScreen.tsx`

- [ ] **Step 1: `frontend/src/components/EndingScreen.tsx` を作成**

```tsx
import { useGameStore } from '../store/useGameStore'
import endingsData from '../data/endings.json'
import type { Ending } from '../types'

const endings = endingsData.endings as Ending[]

export function EndingScreen() {
  const endingId = useGameStore((s) => s.endingId)
  const resetGame = useGameStore((s) => s.resetGame)

  const ending = endings.find((e) => e.id === endingId)
  if (!ending) return null

  const isHappy = ending.id === 'ending-true' || ending.id === 'ending-normal'

  return (
    <div className="fixed inset-0 bg-black/95 flex flex-col items-center justify-center z-40 px-6">
      <div className="max-w-lg w-full text-center">
        <p className="text-purple-400/60 text-xs tracking-widest uppercase mb-3">
          {isHappy ? '— END —' : '— BAD END —'}
        </p>
        <h1 className="text-purple-200 text-xl md:text-2xl font-semibold mb-6 tracking-wide">
          {ending.label}
        </h1>
        <p className="text-purple-100/80 text-sm md:text-base leading-loose mb-10">
          {ending.text}
        </p>
        <button
          onClick={resetGame}
          className="bg-purple-500/10 border border-purple-500/40 rounded-full px-8 py-3 text-sm text-purple-300 hover:bg-purple-500/20 transition-all"
        >
          もう一度はじめる
        </button>
      </div>
    </div>
  )
}
```

- [ ] **Step 2: コミット**

```bash
cd /root/workspace/llm_game
git add frontend/src/components/EndingScreen.tsx
git commit -m "feat: EndingScreen コンポーネント（エンディングテキスト + リセット）"
```

---

## Task 13: GameScreen + App 統合

**Files:**
- Create: `frontend/src/components/GameScreen.tsx`
- Modify: `frontend/src/App.tsx`
- Modify: `frontend/src/main.tsx`

- [ ] **Step 1: `frontend/src/components/GameScreen.tsx` を作成**

```tsx
import { useState } from 'react'
import { StatusBar } from './StatusBar'
import { SceneView } from './SceneView'
import { CommandPanel } from './CommandPanel'
import { SaveLoadModal } from './SaveLoadModal'
import { EndingScreen } from './EndingScreen'
import { useGameStore } from '../store/useGameStore'

export function GameScreen() {
  const [showSave, setShowSave] = useState(false)
  const isEnded = useGameStore((s) => s.isEnded)

  return (
    <div className="flex flex-col h-screen w-screen bg-[#0d0d1a] overflow-hidden">
      <StatusBar onSave={() => setShowSave(true)} onMenu={() => setShowSave(true)} />

      {/* メインシーン */}
      <div className="flex-1 flex flex-col overflow-hidden min-h-0">
        <SceneView />
        <CommandPanel />
      </div>

      {isEnded && <EndingScreen />}
      {showSave && <SaveLoadModal onClose={() => setShowSave(false)} />}
    </div>
  )
}
```

- [ ] **Step 2: `frontend/src/App.tsx` を上書き**

```tsx
import { GameScreen } from './components/GameScreen'

export default function App() {
  return <GameScreen />
}
```

- [ ] **Step 3: `frontend/src/main.tsx` を確認・必要なら修正**

```tsx
import { StrictMode } from 'react'
import { createRoot } from 'react-dom/client'
import './index.css'
import App from './App.tsx'

createRoot(document.getElementById('root')!).render(
  <StrictMode>
    <App />
  </StrictMode>,
)
```

- [ ] **Step 4: ビルド確認**

```bash
cd /root/workspace/llm_game/frontend && npm run build
```

Expected: `dist/` 生成、エラーなし。

- [ ] **Step 5: 開発サーバーで動作確認**

```bash
cd /root/workspace/llm_game/frontend && npm run dev
```

ブラウザで `http://localhost:5173/llm_game/` を開き、ゲーム画面が表示されること・コマンドが動作することを確認。

- [ ] **Step 6: コミット**

```bash
cd /root/workspace/llm_game
git add frontend/src/components/GameScreen.tsx frontend/src/App.tsx frontend/src/main.tsx
git commit -m "feat: GameScreen 統合 + App エントリーポイント"
```

---

## Task 14: CI/CD（GitHub Actions + lefthook）

**Files:**
- Modify: `.github/workflows/ci.yml`
- Create: `.github/workflows/deploy.yml`
- Modify: `lefthook.yml`

- [ ] **Step 1: `.github/workflows/ci.yml` をフロントエンド専用に書き換え**

```yaml
name: CI

on:
  push:
    branches: [main, dev]
  pull_request:

jobs:
  secret-scan:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: read
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683  # v4.2.2
        with:
          fetch-depth: 0
      - uses: gitleaks/gitleaks-action@ff98106e4c7b2bc287b24eaf42907196329070c7  # v2.3.9
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  detect-changes:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: read
    outputs:
      frontend: ${{ steps.filter.outputs.frontend }}
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683  # v4.2.2
      - uses: dorny/paths-filter@de90cc6fb38fc0963ad72b210f1f284cd68cea36  # v3
        id: filter
        with:
          filters: |
            frontend: ['frontend/**']

  frontend-ci:
    needs: detect-changes
    if: needs.detect-changes.outputs.frontend == 'true'
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: frontend
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683  # v4.2.2
      - uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020  # v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: frontend/package-lock.json
      - run: npm ci
      - run: npm run lint
      - run: npx tsc --noEmit
      - run: npm test
      - run: npm run build
```

- [ ] **Step 2: `.github/workflows/deploy.yml` を新規作成**

```yaml
name: Deploy to GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  deploy:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: frontend
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683  # v4.2.2

      - uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020  # v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: frontend/package-lock.json

      - run: npm ci
      - run: npm run build

      - name: Deploy to gh-pages
        uses: peaceiris/actions-gh-pages@4f9cc6602d3f66b9c108549d231e62369c1b4797  # v4
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./frontend/dist
          cname: ''
```

- [ ] **Step 3: `lefthook.yml` に frontend フックを追加**

`lefthook.yml` の `pre-commit.commands` に追記（既存のエントリを保持しつつ）：

```yaml
    tsc-check:
      glob: "frontend/**/*.{ts,tsx}"
      run: cd frontend && npx tsc --noEmit

    eslint-check:
      glob: "frontend/**/*.{ts,tsx}"
      run: cd frontend && npx eslint {staged_files}
```

- [ ] **Step 4: コミット**

```bash
cd /root/workspace/llm_game
git add .github/workflows/ci.yml .github/workflows/deploy.yml lefthook.yml
git commit -m "ci: frontend専用CI + GitHub Pagesデプロイワークフロー追加"
```

---

## Task 15: 動作確認 + スモークテスト

- [ ] **Step 1: テスト全パス確認**

```bash
cd /root/workspace/llm_game/frontend && npm test
```

Expected: 全テストパス。

- [ ] **Step 2: ビルド確認**

```bash
cd /root/workspace/llm_game/frontend && npm run build
```

Expected: `dist/` 生成、エラーなし。

- [ ] **Step 3: 開発サーバーで通しプレイ確認**

```bash
cd /root/workspace/llm_game/frontend && npm run dev
```

`http://localhost:5173/llm_game/` でブラウザ確認：

- [ ] ゲーム画面が表示される
- [ ] コマンドボタンが押せる
- [ ] セリフが変わる
- [ ] ゲージが増減する
- [ ] 3秒クールダウンが動作する
- [ ] SAVEボタンでモーダルが開く
- [ ] セーブ・ロードが動作する
- [ ] trust を 15 以下に下げると BadEnd が表示される
- [ ] スマホ幅（375px）でレイアウトが崩れない

- [ ] **Step 4: 最終コミット**

```bash
cd /root/workspace/llm_game
git add -A
git commit -m "feat: MSSP Phase 1 実装完了"
```
